---
  title: "Transform"
  description: ""
  level: ""
  link_content: "https://twitter.com/alirdev/status/1694635830416175413"
  created_at: 2024-01-01
---

If you are looking to rotate, scale or change the position of an element, then you should learn how to work with transform.

It is a CSS property which is so helpful when making a website, let's go and learn each of them:

- [Rotate](#rotate)
- [Scale](#scale)
- [Translate](#translate)
- [Skew](#skew)

## Rotate

Rotate is one of the value of transform which you can use for rotating your element in any direction.

```css
div {
  transform: rotate(0deg);
  transform: rotateX(0deg);
  transform: rotateY(0deg);
}
```

<ComponentPreview name="transform-rotate" />

How (rotateX) looks like:

<ComponentPreview name="transform-rotate-x" />

What about (rotateY):

<ComponentPreview name="transform-rotate-y" />

## Scale

With scale(), you can resize your element.

```css
div {
  transform: scale(1);
  transform: scaleX(1);
  transform: scaleY(1);
}
```

<ComponentPreview name="transform-scale" />

rotateX()

<ComponentPreview name="transform-scale-x" />

rotateY()

<ComponentPreview name="transform-scale-y" />

## Translate

You can move your element by using translate value.

```css
div {
  transform: translate(0px, 0px);
}
```

<ComponentPreview name="transform-translate" />

## Skew

It defines a transformation that skews an element on the 2D plane.

```css
div {
  transform: skew(0deg);
  transform: skewX(0deg);
  transform: skewY(0deg);
}
```

<ComponentPreview name="transform-skew" />

skewX()

<ComponentPreview name="transform-skew-x" />

skewY()

<ComponentPreview name="transform-skew-y" />

<ComponentPreview name="transform-setting" />
