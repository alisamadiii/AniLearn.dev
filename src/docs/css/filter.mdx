---
title: Filter
tech: CSS
order: 3
---

<Small_Gradient>filter</Small_Gradient>

# Filter Image

The filter CSS property applies graphical effects like blur or color shift to an element. Filters are commonly used to adjust the rendering of images, backgrounds, and borders.

```css:style.css {1} showLineNumbers
img {
    filter: blur(0px); // px
    filter: contrast(100%); // %
    filter: brightness(100%); // %
    filter: Grayscale(0%); // %
    filter: Hue-State(0deg); // deg
    filter: Saturate(100); // Nothing
}
```

## Working Place

This is the place where you can try them.

<Filter />
